<odoo>
	<data>
		<!-- <record id="student_feedback_form" model="ir.rule">
            <field name="name">Student Feedback Record Rule</field>
            <field name="model_id" ref="model_student_feedbackform"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[('student_name.user_id','=',user.id)]</field>
            <field name="groups" eval="[(4, ref('school.group_school_student'))]"/>
        </record> -->
	</data>
</odoo>